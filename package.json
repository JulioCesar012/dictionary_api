{
  "name": "dictionary_api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch ./word_list.json --port 3005",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "body-parser": "1.18.3",
    "cors": "latest",
    "express": "^4.16.4",
    "json-server": "^0.17.0"
  }
}
